{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the daily task sticker windows.",
  "windows": ["main", "config"],
  "permissions": ["core:default", "opener:default", "pilot:default"]
}
